# Architecture

The full architecture pack (overview, diagram overlays, NFR tables, and sequence
flows) lives in the repository root **[README.md](../../README.md)** under the
**Architecture** section.

That section is the single copy of what used to be split across
`overview.md`, `diagram-extensions.md`, `nfr.md`, and `sequence-flows.md`.
