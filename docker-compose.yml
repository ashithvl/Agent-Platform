# Frontend-only stack: builds the SPA and serves it with nginx. No API, workers, or databases.
services:
  web:
    container_name: document-rag-web
    build:
      context: .
      dockerfile: infra/nginx/Dockerfile
    ports:
      - "8080:80"
