/**
 * Content width + horizontal inset used by `PageChrome` and the workflow editor.
 *
 * Pages use the full available width up to a wide maximum (`max-w-screen-2xl`)
 * so large displays are not left with a big dead zone on the right. Vertical
 * spacing lives on the page header / body, not here.
 */
export const WORKSPACE_SHELL_CLASS = "mx-auto w-full max-w-screen-2xl px-6 lg:px-8";
