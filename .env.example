# Document-Rag is intentionally frontend-only.
# `docker compose up --build` builds the Vite SPA and serves it with nginx on http://localhost:8080
# No backend services, databases, or API keys are required for the web app.
